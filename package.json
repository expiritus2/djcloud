{
  "scripts": {
    "start:dev": "lerna run start:dev --parallel",
    "prepare": "husky install",
    "format": "lerna run format --since master --parallel",
    "lint": "lerna run lint --since master --parallel"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": ">=7",
    "lerna": "^5.1.1",
    "lint-staged": ">=10"
  },
  "lint-staged": {}
}
