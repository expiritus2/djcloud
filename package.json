{
  "name": "djcloud-project",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "start:dev": "lerna run start:dev --parallel",
    "prepare": "husky install",
    "format": "lerna run format --since develop --parallel",
    "lint": "lerna run lint --since develop --parallel"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": ">=7",
    "lerna": "^5.1.1",
    "lint-staged": ">=10",
    "prettier": "^2.7.0"
  },
  "lint-staged": {}
}
