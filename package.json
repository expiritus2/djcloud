{
  "scripts": {
    "start:dev": "lerna run start:dev --parallel",
    "prepare": "husky install",
    "format": "lerna run format --since --parallel",
    "lint": "lerna run lint --since --parallel"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": ">=7",
    "lerna": "^5.1.1",
    "lint-staged": ">=10"
  },
  "lint-staged": {}
}
